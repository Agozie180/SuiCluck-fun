import { Flame, TrendingUp } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
export function CurveCard({ name, ticker, progress, raised }: { name: string; ticker: string; progress: number; raised: string }) { return <Card className="overflow-hidden"><CardContent className="p-4"><div className="flex items-start justify-between gap-4"><div className="flex gap-3"><div className="grid size-14 place-items-center rounded-md bg-secondary text-3xl">🐔</div><div><p className="font-display text-xl font-black">{name}</p><p className="text-sm font-bold text-secondary">${ticker}</p></div></div><Flame className="text-accent"/></div><div className="mt-4"><div className="mb-2 flex justify-between text-sm"><span className="text-muted-foreground">Graduation</span><span className="font-bold">{progress}%</span></div><div className="h-3 overflow-hidden rounded-full bg-white/10"><div className="h-full rounded-full bg-gradient-to-r from-primary via-secondary to-accent" style={{ width: `${progress}%` }}/></div></div><div className="mt-4 flex items-center justify-between text-sm"><span className="flex items-center gap-1 text-muted-foreground"><TrendingUp size={15}/>Raised</span><span className="font-bold">{raised}</span></div></CardContent></Card> }
